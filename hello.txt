name = ARGV.first || "World"

puts "Hello, #{name}!"

# Default is "World"
name = ARGV.first || "World"

puts "Hello, #{name}!"

# Default is World
# Author: Roman Mokrivskiy
name = ARGV.first || "World"

puts "Hello, #{name}!"

# Default is World
# Author: Mokrivskiy Roman (mokrivskiyroman@gmail.com)
name = ARGV.first || "World"

puts "Hello, #{name}!"
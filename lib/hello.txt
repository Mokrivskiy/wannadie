name = ARGV.first || "World"

puts "Hello, #{name}!"

# Default is "World"
name = ARGV.first || "World"

puts "Hello, #{name}!"

# Default is World
# Author: Roman Mokrivskiy
name = ARGV.first || "World"

puts "Hello, #{name}!"

# Default is World
# Author: Mokrivskiy Roman (mokrivskiyroman@gmail.com)
name = ARGV.first || "World"

puts "Hello, #{name}!"

puts "What's your name"
my_name = gets.strip

puts "Hello, #{my_name}!"